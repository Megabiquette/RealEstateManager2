<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:orientation="vertical"
	android:layout_width="@dimen/item_media_width_height"
	android:layout_height="@dimen/item_media_width_height"
	android:layout_margin="@dimen/media_item_padding" >

	<ImageView
		android:id="@+id/fragment_property_card_item_imageView"
		android:layout_width="match_parent"
		android:layout_height="match_parent"/>

	<TextView
		android:id="@+id/fragment_property_card_item_description"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_alignParentBottom="true"
		android:textColor="@color/background_light"
		android:padding="@dimen/media_item_description_padding"
		android:background="#4C000000"/>

</RelativeLayout>